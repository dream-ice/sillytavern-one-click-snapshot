# 更新日志

## 0.7.0 — 2026-08-20

**在快照内容里精细修改**（可在设置里关闭）

- 「查看内容」页新增「修改」按钮，可以直接改这个快照记录了什么，改动只写进快照，不会动你当前正在用的状态。
- 世界书：换成另一本、逐条勾选条目、删除、按子域分组添加；条目按预设转移 / 柏宝箱的分组折叠，每组一个开关。
- 角色 / 用户版本：先选人再选版本，也可以换成另一个角色或用户。
- 界面美化：换成另一个主题。
- 预设：换成另一个预设，逐条勾选条目并按分组折叠，生成参数也可以逐项修改。
- 正则：先选角色或预设，再加载它的规则来挑；三类来源可以单独移除和加回。
- 可以增删记录范围，新增的范围是空的，加进来之后在这一页挑内容。
- 取消会把这一次进入修改模式后的所有改动一并撤销。

**修复**

- 应用快照时，判断「这个快照属于哪个角色」改为从记录的世界书本身反推，手工组装的快照不会再漏掉不匹配提示。
- 原版预设转移用户应用快照时不再弹出分组警告，也不会再跳过所有已分组的世界书条目。
- 关闭「User标签管理」后，插件加入的排序方式会真正从下拉框里移除，而不只是置灰。
- 用户管理的文件夹改为与用户卡同排显示，网格视图下的排版、返回键和头像对齐都与角色列表一致。
- 收藏描边跟随头像形状，方形头像不再显示圆环。
- 快照库与版本库的排序框回到右侧，移动端搜索行撑满宽度。
- 修复「查看内容」页多处右端被截断：单列 grid 的隐式轨道按最大内容撑开，一个长条目就会把整列顶出弹窗。
- 展开条目后的箭头会翻转，各处计数标注了单位。

## 0.6.0 — 2026-08-06

**功能设置**

- 扩展程序页新增「一键快照」设置面板，状态快照、U/C多版本、开场白管理、User标签管理、原生小优化各有一个总开关。
- 所有开关即时生效，不用刷新页面。
- 关掉某个功能后，它在酒馆各处加的入口和选项一并消失。
- 每项说明收进名字旁边的 ⓘ，点开才看。

**范围编辑与刷新分家**

- 快照卡片上的范围徽章改为点了立刻生效，且只处理你点的那一个——加一个范围只录入这一个，删一个只丢这一个，其余范围原样不动。
- 「更新」按钮从此只做刷新，不会再改变快照记录了哪些范围。
- 批量操作新增「移除范围」，可以一次性从多个快照里删掉某个范围。
- 可选「更新时询问范围」：更新前先让你挑这次刷新哪些，没挑的保持原样，单个和批量各有开关。
- 世界书和正则可以细选到具体的某几本、某几条。
- 可选「允许给快照补上新范围」：开启后批量更新会给缺少该范围的快照补上，关闭则跳过它们。

**快照库与版本库**

- 快照库改为平铺分页列表，卡片可折叠，支持一键全部展开 / 折叠。
- 快照批量操作：更新、移除范围、分组、删除。
- 版本管理同样加上分页、排序、展开折叠、批量分组 / 删除和分组筛选。
- 新增六种排序，其中「最近应用」按你上次应用的时间排。
- 正在应用的快照和版本固定置顶。

**其他**

- 可选「保存快照时自动建初始版本」：快照勾了角色/用户版本但还没有任何版本时，自动按当前描述建一个「初始版本」并指向它。
- 预设的提示词框不再一写 `{{setvar::}}` 就自动弹宏说明，改为跟随用户设置里的「Show in all macro fields」，Ctrl+Space 仍可手动调出。
- 开场白管理面板里，绑定了快照的开场白会直接标出是哪一个。
- 修复移动端快照面板两个标签页右侧没有对齐的问题。
- 修复版本卡片的更新时间挤在名字旁边、没有右对齐的问题。

## 0.5.0 — 2026-08-03

- 用户角色批量操作补齐收藏、打标签、复制、删除，顺序和角色卡右键菜单一致。
- 角色卡批量模式下，把右键菜单里的收藏、标签、复制、转为用户做成看得见的按钮。
- 正则的「替换为」「修剪掉」加上展开按钮，长内容可以全屏编辑。
- 用户列表置顶当前角色绑定的那个用户，切到别的用户看参考也能一键切回。
- 复制用户角色时不再继承原来的锁定关系和版本引用。
- 批量操作未选中时显示 0，和角色卡一致。
- 收藏描边换成独立配色，不再和系统默认用户的金框混淆。

## 0.4.0 — 2026-08-02

- 新增用户角色管理：标签、文件夹、收藏、批量选择/删除，九种排序。
- 用户的标签独立存放，不会污染角色卡的标签筛选。
- 按聊天层数、Token 数排序，扫描聊天记录时只读不写。
- 版本管理新增开关，可把版本名写进原生的「备注」/「角色版本」，在列表里直接看到当前版本。
- 快照库加上搜索。
- 要求酒馆 1.18.0 或更高版本。

## 0.3.x — 2026-07

- 首个版本：保存和一键还原角色、用户、预设、世界书、正则等整套状态。
- 角色和用户的多版本管理，含版本头像、描述对比。
- 开场白管理：搜索、分组、拖拽排序、批量操作、绑定快照。
- 快照与版本分组，正在应用的项目置顶。
- 移动端界面优化。
